package com.tonkar.volleyballreferee.engine.game;

public enum ActionOriginType {
    // Provide feedback to the user when the application performs the action
    // Provide none when the user performs the action (as he is aware of it)
    USER,
    APPLICATION
}
